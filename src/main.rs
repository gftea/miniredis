// use tokio::fs::File;
// use tokio::io::{self, AsyncReadExt};

use std::cmp;

use bytes::{Buf, Bytes, BytesMut, BufMut};




fn main() {
    let mut a = [0u8;10];
    a[0] = 1;
    
    let mut sl = &mut [0u8;10][..];
    sl[0] = 1;

}
